" -*- Vimrc -*-

" Always show the gitgutter column
set signcolumn=yes

" Fire CursorHold 250ms after we stop doing things, not 4s (the default)
" This is when gitgutter will update itself
let updatetime=250
