# github:etc/bash/login/00.placeholder
# -*- Mode: Bash; tab-width: 4; indent-tabs-mode: nil; -*-
# vim: set ft=sh: 
#
#       This contains nothing, but illustrates where login items are placed.

available "mvim" && alias :e="mvim --remote-silent" || ( \
  available "gvim" && alias :e="gvim --remote-silent" || ( \
  alias :e="vim" ) )

